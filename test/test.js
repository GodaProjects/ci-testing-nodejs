const assert = require('assert')


describe('Goda', function () {
    describe('Tanu', function () {
        it('Should return -1 when the value is not present', function () {
            assert.equal(['goda', 'tanu', 'rama'].indexOf(4), -1)
        })
    })
})